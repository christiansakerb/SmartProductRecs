import sys
import os
import pandas as pd
import time
import yaml,joblib
from sdk.sdk import executeQueryOracle,ExecuteSimpleOracle,ExecuteManyOracle

# Añadir la ruta de la carpeta anterior al sys.path

# Ahora puedes importar el módulo

def Generar_query(nombre,query_text='',filtro='',verbose=True):

    #Código elimninado por ser de uso privado
    #Código elimninado por ser de uso privado
    #Código elimninado por ser de uso privado

    df = executeQueryOracle(query_text)

    return df

